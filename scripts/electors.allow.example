# ~/.conclave/electors.allow —— 允许其区块进入本地账本的节点
#
# 每行一个 elector 指纹（16 位十六进制，在对方 UI 左上角 "elector xxx" 处）。
# 本节点自己永远在名单内，不用写。文件不存在 = 只信任自己 = 单机模式。
#
# ⚠️ 这道闸决定了「谁的评审任务可以在你的机器上跑 Bash」。只加同一团队、
#    本来就都有对应 repo 权限的人的机器。改动即时生效，不用重启。

# 35641f207ebfe263   # alan 的 MacBook
# a1b2c3d4e5f60789   # edison 的 MacBook
